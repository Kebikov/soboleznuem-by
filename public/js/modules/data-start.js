function dataStart() {
    const work = document.querySelector('[data-script="data-start"]');
    if(work) {
        const infoBtn = document.querySelector('.data-start__info');
        const replaceBtn = document.querySelector('.data-start__replace');

        infoBtn.addEventListener('click', () => {
            
        });

        replaceBtn.addEventListener('click', () => {

        });
    }
}